"use strict";
var _configContentful = require("../config/contentful");
const entryID = "PJ2glfLdwqRn1eJiOUCAp";
const link = `https://cdn.contentful.com/spaces/${_configContentful.spaceID}/environments/master/entries/${entryID}?access_token=${_configContentful.accessToken}`;
window.addEventListener("load", start);
function start() {
  console.log("start");
  // show data at load
  loadJSON(link, showData);
}
// loads data
function loadJSON(url, callback) {
  fetch(url).then(response => response.json()).then(jsonData => {
    callback(jsonData);
  });
}
function showData(data) {
  console.log(data);
  document.querySelector(".splash-text").textContent = data.fields.header;
}
